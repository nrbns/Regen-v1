/**
 * AIResponsePane (stub)
 * Original implementation moved to src/_deferred/ai/AIResponsePane.tsx
 * This lightweight stub preserves the named export to avoid breaking imports
 * while keeping the active bundle minimal for v1.
 */

import React from 'react';

interface AIResponsePaneProps {
  query: string;
  isOpen: boolean;
  onClose: () => void;
}

export function AIResponsePane(_props: AIResponsePaneProps) {
  return null;
}
