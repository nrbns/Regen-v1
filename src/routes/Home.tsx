// Home component - now handled by AppShell directly
// This component is kept for routing compatibility but is no longer used

export default function Home() {
  // Browser functionality is now handled directly in AppShell
  // This component is kept for routing compatibility
  return null;
}
