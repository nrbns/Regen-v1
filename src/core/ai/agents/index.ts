/**
 * Controlled Agents - Public API
 * 
 * Agents in Regen are controlled, visible, and bounded tasks.
 * They are NOT autonomous - they are looping tasks under strict rules.
 */

export * from './ControlledAgent';
export * from './types';
export { ControlledAgent } from './ControlledAgent';
